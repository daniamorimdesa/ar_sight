/// Datasource para comunicação HTTP (preparado para gRPC no futuro)
class SceneDatasource {
  // TODO: Implementar chamadas HTTP
  
  Future<Map<String, dynamic>> sendSceneForDiagnosis(String imagePath) async {
    throw UnimplementedError('sendSceneForDiagnosis não implementado');
  }
}
